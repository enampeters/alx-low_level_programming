#include "main.h"

/**
 * mul - multiplies up two numbers
 * @a: first number in the product process
 * @b: second number to be multiplied
 *
 * Return: the product of two numbers
 */

int mul(int a, int b)
{
	return (a * b);
}
