#include <stdio.h>

/**
 * main - Entry point
 * Return: always 0 (Success)
 * puts function
 * doesnt require /n
 */
int main(void)
{
	/**
	 * setting up string value to be used in puts
	 */
	char Mystr[] = "\"Programming is like building a multilingual puzzle";

	/**
	 * writing to stdout
	 */
	puts(Mystr);

	return (0);
}
