#include <stdio.h>

/**
 * main - Entry piont
 * Return: Always 0 (Success)
 * A printf test
 */
int main(void)
{
	/**
	 * Getting stout
	 */
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");

	return (0);
}

